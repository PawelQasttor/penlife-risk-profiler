steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/penlife-risk-profiler:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/penlife-risk-profiler:latest'
      - '.'

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/penlife-risk-profiler:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/penlife-risk-profiler:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'penlife-risk-profiler'
      - '--image=gcr.io/$PROJECT_ID/penlife-risk-profiler:$COMMIT_SHA'
      - '--platform=managed'
      - '--region=europe-west2'
      - '--allow-unauthenticated'
      - '--memory=2Gi'
      - '--cpu=2'
      - '--timeout=300'
      - '--max-instances=10'
      - '--service-account=penlife-risk-profiler@$PROJECT_ID.iam.gserviceaccount.com'
      - '--set-env-vars=GOOGLE_CLOUD_PROJECT=$PROJECT_ID'

images:
  - 'gcr.io/$PROJECT_ID/penlife-risk-profiler:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/penlife-risk-profiler:latest'

options:
  logging: CLOUD_LOGGING_ONLY
